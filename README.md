# Mini Notes API (Node.js Workshop)

A simple CRUD API built with **Node.js + Express**, paired with a lightweight HTML frontend for testing the endpoints.

---

## 🚀 Features

- Get all notes
- Get a single note by ID
- Add a new note
- Update a note
- Delete a note
- Simple HTML frontend using `fetch()`
- No database required (in-memory array)

---

## 📁 Structure

- server.js
- index.html
- package.json
- README.md

---

### 🛠 Setup

#### 1. Clone the repository and navigate into the project directory:

- git clone https://github.com/zaybournand/Node-Workshop-Node.git
- cd Node-Workshop-Node

#### 2. Install Dependencies:

- npm install
- npm install --save-dev nodemon

#### 3. ▶️ Running the Application

- npm start or npm run dev (Available at http://localhost:3000/)
- Click 'Go Live' on VS Code (bottom-right in VS Code)

## 🎯 Note

- Data is stored in-memory (resets on server restart)
